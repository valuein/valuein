{
  "version": "3.3.0",
  "project": "Valuein SEC Data Warehouse",
  "tables": {
    "references": {
      "description": "Denormalized flat join of entity + security.  One row per security — covers the company universe (CIK + primary identifiers + sector + ticker).  For ANY membership question (current or historical, SP500 or Russell), JOIN with index_membership on cik = cik.  The join column has the same name on both sides — this view does NOT carry index-membership flags because they're inherently snapshot-only and single-index, both of which are footguns the index_membership table avoids by design.",
      "columns": {
        "cik": {
          "type": "VARCHAR"
        },
        "name": {
          "type": "VARCHAR"
        },
        "sector": {
          "type": "VARCHAR"
        },
        "industry": {
          "type": "VARCHAR"
        },
        "sic_code": {
          "type": "VARCHAR"
        },
        "status": {
          "type": "VARCHAR"
        },
        "entity_type": {
          "type": "VARCHAR"
        },
        "security_id": {
          "type": "INTEGER"
        },
        "symbol": {
          "type": "VARCHAR"
        },
        "exchange": {
          "type": "VARCHAR"
        },
        "mic": {
          "type": "VARCHAR"
        },
        "valid_from": {
          "type": "DATE"
        },
        "valid_to": {
          "type": "DATE"
        },
        "is_active": {
          "type": "BOOLEAN"
        },
        "figi": {
          "type": "VARCHAR"
        },
        "composite_figi": {
          "type": "VARCHAR"
        },
        "share_class_figi": {
          "type": "VARCHAR"
        },
        "is_primary_ticker": {
          "type": "BOOLEAN",
          "description": "Propagated from security.is_primary_ticker.  TRUE for the entity's primary listing.  Multi-share-class issuers (BRK-A/BRK-B, GOOG/GOOGL) emit multiple rows here; default consumer queries (and the MCP universe dedup) filter is_primary_ticker=TRUE for one row per CIK."
        }
      }
    },
    "entity": {
      "description": "Legal structure and profile of reporting companies.",
      "columns": {
        "cik": {
          "type": "VARCHAR",
          "primary_key": true
        },
        "name": {
          "type": "VARCHAR"
        },
        "lei": {
          "type": "VARCHAR"
        },
        "industry": {
          "type": "VARCHAR"
        },
        "sector": {
          "type": "VARCHAR"
        },
        "sic_code": {
          "type": "VARCHAR"
        },
        "sic_description": {
          "type": "VARCHAR",
          "description": "Full SEC SIC industry label (e.g. 'Electronic Computers').  Companion to ``sic_code`` — use this on responses where a human-readable string is more useful than the raw 4-digit code."
        },
        "fiscal_year_end": {
          "type": "VARCHAR"
        },
        "location": {
          "type": "VARCHAR"
        },
        "website": {
          "type": "VARCHAR"
        },
        "ceo": {
          "type": "VARCHAR"
        },
        "founded_year": {
          "type": "SMALLINT"
        },
        "description": {
          "type": "VARCHAR"
        },
        "status": {
          "type": "VARCHAR"
        },
        "entity_type": {
          "type": "VARCHAR"
        },
        "ein": {
          "type": "VARCHAR"
        },
        "owner_org": {
          "type": "VARCHAR"
        },
        "investor_website": {
          "type": "VARCHAR"
        },
        "category": {
          "type": "VARCHAR"
        },
        "state_of_incorporation": {
          "type": "VARCHAR"
        },
        "state_of_incorporation_description": {
          "type": "VARCHAR",
          "description": "Full SEC label for the state-of-incorporation code (e.g. 'Delaware')."
        },
        "phone": {
          "type": "VARCHAR"
        },
        "business_address": {
          "type": "VARCHAR"
        },
        "mailing_address": {
          "type": "VARCHAR",
          "description": "Mailing address (separate from HQ) — registered agent / correspondence target.  Often differs from business_address for foreign issuers and trusts."
        },
        "country_code": {
          "type": "VARCHAR"
        },
        "is_foreign": {
          "type": "BOOLEAN"
        },
        "flags": {
          "type": "VARCHAR",
          "description": "SEC-flagged status string (e.g. 'Shell', 'Delinquent'). NULL for clean issuers."
        },
        "has_insider_transactions": {
          "type": "BOOLEAN",
          "description": "SEC declares Form 4/5 activity exists for this issuer."
        },
        "is_insider_owner": {
          "type": "BOOLEAN",
          "description": "SEC declares this CIK itself owns insider holdings."
        },
        "former_names": {
          "type": "JSON",
          "description": "SEC ``formerNames[]`` history — JSON array of ``{name, from, to}`` objects.  Useful for ticker-history queries and merger lineage (e.g. tracing FB → META, GOOG → GOOGL share splits, or a former subsidiary's prior name).  Empty array when the issuer has never renamed."
        },
        "last_seen_at": {
          "type": "DATE"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "security": {
      "description": "Ticker symbols and tradeable instrument metadata (SCD Type 2 with date ranges).",
      "columns": {
        "id": {
          "type": "INTEGER",
          "primary_key": true
        },
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "symbol": {
          "type": "VARCHAR"
        },
        "exchange": {
          "type": "VARCHAR"
        },
        "mic": {
          "type": "VARCHAR"
        },
        "valid_from": {
          "type": "DATE"
        },
        "valid_to": {
          "type": "DATE"
        },
        "is_active": {
          "type": "BOOLEAN"
        },
        "figi": {
          "type": "VARCHAR"
        },
        "composite_figi": {
          "type": "VARCHAR"
        },
        "share_class_figi": {
          "type": "VARCHAR"
        },
        "security_type": {
          "type": "VARCHAR"
        },
        "market_sector": {
          "type": "VARCHAR"
        },
        "figi_ticker": {
          "type": "VARCHAR"
        },
        "figi_name": {
          "type": "VARCHAR"
        },
        "figi_exch_code": {
          "type": "VARCHAR"
        },
        "is_primary_ticker": {
          "type": "BOOLEAN",
          "description": "True for the ticker SEC's company_tickers.json marks as the entity's primary listing.  Multi-share-class issuers (BRK-A/BRK-B, GOOG/GOOGL) emit multiple is_active=TRUE rows; default consumer queries filter is_primary_ticker=TRUE for one row per CIK."
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "filing": {
      "description": "Metadata for all SEC filings processed.",
      "columns": {
        "accession_id": {
          "type": "VARCHAR",
          "primary_key": true
        },
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "filing_date": {
          "type": "DATE"
        },
        "form_type": {
          "type": "VARCHAR"
        },
        "filing_url": {
          "type": "VARCHAR"
        },
        "is_amendment": {
          "type": "BOOLEAN"
        },
        "amendment_no": {
          "type": "SMALLINT"
        },
        "superseded_by": {
          "type": "VARCHAR",
          "references": "filing.accession_id",
          "description": "Accession-id of the filing that supersedes this one in the amendment chain (NULL when this is the latest version).  Populated post-ingest by ``services/filings/amendment_linker.py``."
        },
        "report_date": {
          "type": "DATE"
        },
        "items": {
          "type": "VARCHAR[]"
        },
        "size": {
          "type": "INTEGER",
          "description": "Filing size in bytes (per SEC submissions feed)."
        },
        "is_xbrl": {
          "type": "BOOLEAN",
          "description": "SEC-declared: filing carries an XBRL exhibit."
        },
        "is_inline_xbrl": {
          "type": "BOOLEAN",
          "description": "SEC-declared: filing uses Inline XBRL (iXBRL)."
        },
        "is_xbrl_numeric": {
          "type": "BOOLEAN",
          "description": "Distinct from is_xbrl/is_inline_xbrl — TRUE when the XBRL exhibit carries machine-readable numeric facts (the source of every fact-table row)."
        },
        "primary_document": {
          "type": "VARCHAR",
          "description": "Filename of the lead document in the filing package."
        },
        "primary_doc_description": {
          "type": "VARCHAR",
          "description": "SEC-provided label describing the primary document."
        },
        "core_type": {
          "type": "VARCHAR",
          "description": "Base form_type stripped of the ``/A`` amendment suffix (e.g. ``10-K/A`` → ``10-K``).  Use this for form-family filtering when you don't care whether a filing is the original or an amendment."
        },
        "file_number": {
          "type": "VARCHAR",
          "description": "SEC EDGAR file number (e.g. ``001-36743``)."
        },
        "film_number": {
          "type": "VARCHAR",
          "description": "SEC EDGAR microfiche / film number assigned at acceptance."
        },
        "act": {
          "type": "VARCHAR",
          "description": "Securities-Act designator: ``33`` (1933 Act), ``34`` (1934 Act), or empty.  Helps distinguish registration-statement vs. exchange-act filings."
        },
        "is_audited": {
          "type": "BOOLEAN",
          "description": "Filing is an annual (audited) report — TRUE for 10-K / 20-F / 40-F and their amendments, FALSE for 10-Q / 8-K / etc."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "fact": {
      "description": "Standardized financial data points (US-GAAP XBRL mapped to canonical standard_concept labels).",
      "columns": {
        "fact_id": {
          "type": "VARCHAR",
          "description": "Deterministic identity hash: SHA-256(entity_id|accession_id|concept|period_end|unit). Matches the Postgres unique constraint idx_fact_unique_constraint. Computed at export time by run_exports.py — not stored in the database."
        },
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "accession_id": {
          "type": "VARCHAR",
          "references": "filing.accession_id"
        },
        "concept": {
          "type": "VARCHAR"
        },
        "standard_concept": {
          "type": "VARCHAR"
        },
        "priority": {
          "type": "SMALLINT"
        },
        "label": {
          "type": "VARCHAR"
        },
        "value": {
          "type": "VARCHAR",
          "description": "Raw as-reported value string, before numeric parsing.  Holds text for non-numeric facts (e.g. dei cover-page strings); for numeric facts prefer ``numeric_value``."
        },
        "numeric_value": {
          "type": "DOUBLE",
          "description": "The value as reported in this specific filing (accession_id)."
        },
        "derived_quarterly_value": {
          "type": "DOUBLE"
        },
        "unit": {
          "type": "VARCHAR"
        },
        "period_start": {
          "type": "DATE"
        },
        "period_end": {
          "type": "DATE"
        },
        "fiscal_year": {
          "type": "INTEGER"
        },
        "fiscal_period": {
          "type": "VARCHAR"
        },
        "frame": {
          "type": "VARCHAR"
        },
        "statement_type": {
          "type": "VARCHAR"
        },
        "accuracy_score": {
          "type": "DOUBLE",
          "description": "Legacy 0-1 standardization-match score stamped by the in-memory resolver at ingest.  Superseded for consumer filtering by ``confidence_score`` (the composite quality score); kept for traceability of the concept mapping."
        },
        "confidence_score": {
          "type": "DOUBLE",
          "description": "Per-fact confidence in [0, 1] — a PENALTY FROM EVIDENCE, not an opinion. Every fact starts at 1.0 and is docked only for something we can point at. Formula (v2): 1.0 - 0.40 * min(identity_violation_count / 3, 1) - 0.20 * min(restatement_count / 5, 1) - 0.05 * (NOT is_audited). Every input ships in this same table, so you can RECOMPUTE this number and check us — that is deliberate. Read it as: >= 0.95 we hold no evidence against this fact (the normal case, ~99.9% of rows, across all years); ~0.80 a later filing revised it (see the restatement diff on /radar); < 0.60 an accounting identity FAILED on its filing. NB there is deliberately no age term: an older filing is not less trustworthy, it is more settled, and a score that decays with age would turn `>= 0.95` into a recency filter on a point-in-time, survivorship-free dataset."
        },
        "reliability_code": {
          "type": "TINYINT",
          "description": "NOMINAL reliability bucket (1-4) for Bloomberg PR / Refinitiv vendor parity: 1=preliminary (identity errors present), 2=announced (clean, unaudited), 3=audited (audited + clean), 4=restated (audited + clean + multiple versions on file). ⚠️ NOT AN ORDINAL — 4 ('restated') is not 'better' than 3 ('audited, clean'), despite being the larger number, so `WHERE reliability_code >= 3` silently INCLUDES restated facts. To rank by quality, order by confidence_score; use this column only for equality tests against a specific bucket."
        },
        "identity_violation_count": {
          "type": "SMALLINT",
          "description": "Number of error-severity accounting identities that fired against this fact's filing.  Sourced from qa_violation."
        },
        "restatement_count": {
          "type": "SMALLINT",
          "description": "Number of times this fact has been re-reported across accessions. Grain is (entity, RAW XBRL concept, period_start, period_end, unit) — sourced from fact_lineage_summary.version_count - 1. Zero = never restated; > 0 = restated N times. NB the grain is the raw filer tag and the reporting WINDOW, never our standardized concept (a mapping collapses ~12k tags onto ~283 names, so grouping on it diffs values that were never comparable) and never fiscal_period (a derived LABEL, sometimes missing and sometimes wrong — putting it in a grain splits one fact into several). Both mistakes INVENT restatements."
        },
        "is_negative": {
          "type": "BOOLEAN"
        },
        "is_audited": {
          "type": "BOOLEAN"
        },
        "is_estimated": {
          "type": "BOOLEAN",
          "description": "TRUE when part of the published value was INFERRED rather than read from the source. In practice this means a monetary SCALE: the EX-27 Financial Data Schedules that carry our 1994-2001 history omit their <MULTIPLIER> tag on ~27% of schedules, and a missing scale is invisible to every accounting identity because every line is wrong by the same factor. Where the scale was pinned by an anchor outside the monetary block (the filer's own declaration on another schedule, or the EPS-implied share count) the fact ships with this flag set and a confidence_score deduction; where it could not be pinned, the monetary fact was not published at all. FALSE on every XBRL-sourced fact."
        },
        "data_quality": {
          "type": "VARCHAR",
          "description": "How this number was obtained. 'standard' = read from an XBRL exhibit the filer tagged (2009-present; the overwhelming majority of the table). 'ex27_fds' = parsed from an SEC EX-27 Financial Data Schedule, the tagged machine-readable summary that Reg S-T Rules 401/402 required on every 10-K and 10-Q from 1994-09-01 until the rescission effective 2001-01-01 — still the filer's own tagged number, one layer of our parsing away. 'html_derived' = extracted by us from the filing's rendered financial statements (2001-2008, where no machine-readable exhibit exists). 'sgml_derived' = extracted by us from the fixed-width SGML statements of a pre-2003 filing, sliced at the filer's own column offsets. 'human_verified' = a value a named reviewer read off the filing by hand and recorded with the page/line evidence and a sec.gov link (the evidence is in `label`; the raw tag is 'human:<concept>'); it replaces the parser lanes' value for that series, is never estimated, ships at confidence 1.0, and is still outranked by an XBRL value for the same series. Filter on this column to restrict a backtest to XBRL-era facts; the confidence_score already carries a deduction for the other two, and the raw source tag is preserved in `concept` (an 'fds5:'-style prefix) so any recovered value traces to a specific line of a specific exhibit."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        },
        "accounting_standard": {
          "type": "VARCHAR",
          "description": "Reporting taxonomy of the source filing — 'US-GAAP' or 'IFRS'.  Derived from the XBRL taxonomy; defaults 'US-GAAP' for 10-K/10-Q/8-K and is usually 'IFRS' for 20-F / 40-F foreign issuers."
        },
        "reporting_currency": {
          "type": "VARCHAR",
          "description": "ISO-4217 currency code when the unit is a monetary currency (e.g. 'USD', 'EUR', 'JPY').  NULL for non-monetary facts (shares, ratios, days, etc.)."
        },
        "period_span_days": {
          "type": "INTEGER"
        },
        "is_cumulative": {
          "type": "BOOLEAN"
        },
        "derivation_type": {
          "type": "VARCHAR"
        },
        "derivation_uncertain": {
          "type": "BOOLEAN"
        },
        "value_current": {
          "type": "DOUBLE",
          "description": "Bloomberg Option-C: the latest-known value for this (entity_id, standard_concept, period_end, unit) — i.e., the value any analyst watching the tape TODAY would see. Computed at export time via DuckDB window functions."
        },
        "value_as_filed": {
          "type": "DOUBLE",
          "description": "Bloomberg Option-C: the originally-reported value (first-ever accepted_at in the partition)."
        },
        "first_filed_at": {
          "type": "TIMESTAMPTZ",
          "description": "Timestamp of first disclosure (min accepted_at in the partition)."
        },
        "restated": {
          "type": "BOOLEAN",
          "description": "TRUE if any version of this fact in the partition differs from the others — signals that a later filing restated this number.  WARNING: this signal is computed against the GLOBAL state of the warehouse, NOT PIT — a query with as_of_date in the past may still see TRUE for a fact whose restatement happened AFTER the cutoff.  Treat as informational; for PIT reasoning, the MCP server exposes this as ``lineage.restated_in_warehouse`` so consumers know the field is not PIT-safe (audit CC-004)."
        }
      }
    },
    "index_membership": {
      "description": "Index constituents keyed on ``cik``.  Always JOIN ``references.cik = index_membership.cik`` to attach company metadata (name, sector, ticker).  Filter by ``index_name`` for the index, by ``effective_date`` / ``removal_date`` for the as-of window (half-open ``[)`` semantics).  Note: ``cik`` is named ``cik`` here (NOT ``entity_id`` like security/filing/fact) so the JOIN with ``references`` uses the same column name on both sides — see migration 0015 for the rationale.\n\nHISTORICAL DEPTH AND PROVENANCE DIFFER BY INDEX — read this before using the table for point-in-time work.  The ``source`` and ``confidence`` columns are the discriminators, and they are the ones to branch on.\n* **SP500 — from 1996-01-02, ``high`` confidence.**  1,014 entry/exit spells over 968 CIKs, 510 of them closed, including companies long delisted, with ``removal_date`` and ``removal_reason`` populated.  ``source`` is ``historical_seed`` / ``public_constituent_list`` / ``succession_config``.\n* **RUSSELL1000 / RUSSELL2000 / RUSSELL3000 — from 2000-09-30, ``medium`` confidence, ``source='fund_holdings'``.**  3,051 / 10,130 / 11,025 spells, of which 2,059 / 8,214 / 8,117 are closed.  Reconstructed from the publicly disclosed portfolio holdings of large funds tracking each index, NOT from the index provider's own constituent lists, which are licensed IP we do not redistribute.\n\nThree bounds follow from that derivation and must be carried into any conclusion drawn from the Russell rows.  (1) OBSERVATION SPACING VARIES — roughly one to four observations a year from 2000 through 2006, monthly from 2007 on — so a join or leave date is only as precise as the interval between observations, and is never exact to the day in the early years.  (2) A tracking fund only PROXIES its index — a few holdings sit outside it and a sampled fund can miss members — so these are our derived membership, not the index.  (3) There is NO Russell data before 2000-09-30; an earlier date returns zero rows because we do not carry it, which is not a statement that the index was empty.  Confidence is therefore ``medium`` and never ``high``, which correctly leaves the MCP's ``pit_safe`` false.\n\nMEASURED against the index provider's 13 published reconstitution lists (used to grade, never published): recall 96.5%–98.3% per year on RUSSELL3000, worst 96.5%.  Precision is 93.6%–98.6% once the official list's ~22-character truncated company names are accounted for; the unadjusted floor reads 87.3%–97.7% and understates early years, because a company renamed since the list date cannot be matched at all.\n\nKNOWN GAP — no holdings observation exists anywhere between 2016-12-30 and 2017-07-31, a 213-day window, for any fund read.  Russell membership cannot be observed there, so departures collapse onto 2016-12-31 and arrivals onto 2017-07-31, and the June 2017 reconstitution therefore appears to happen on 31 July.  A year-end 2016 point-in-time query returns 2,623 RUSSELL3000 members rather than ~2,900 for this reason alone.  SP500 is unaffected.\n\nSP500 pre-2007 coverage rises 1996 ≈341 members → 2006 ≈454 against a real ~500.  That shortfall is a CIK RESOLUTION ceiling, not the index's size: pre-XBRL delisted members were never ingested into ``entity``, and this table's ``cik`` is an FK to it.",
      "columns": {
        "id": {
          "type": "INTEGER",
          "primary_key": true
        },
        "cik": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "index_name": {
          "type": "VARCHAR",
          "description": "Canonical short code.  Exactly four values are populated: SP500 | RUSSELL1000 | RUSSELL2000 | RUSSELL3000.  Depth and provenance differ per index — see the table description."
        },
        "effective_date": {
          "type": "DATE",
          "description": "First trading day the company traded as a member."
        },
        "announcement_date": {
          "type": "DATE",
          "description": "Date the index provider publicly announced the addition — intended for inclusion-arb work, where the news-public date matters rather than the first trading day.  Typically 5–10 trading days before ``effective_date``.\n\n⚠️ CURRENTLY NULL ON EVERY ROW — 25,220 of 25,220, verified 2026-09-16.  No announcement source has ever contributed a row, so ``get_pit_universe`` with ``as_of_basis='announcement'`` returns ZERO members for EVERY index and every date.  It fails silently — an empty universe, not an error.  **Use the default effective basis.**  Treat this column as reserved-but-unpopulated until a source fills it; the Russell rows could not carry it in any case, being derived from a fund holdings panel that has no announcement concept at all."
        },
        "removal_date": {
          "type": "DATE",
          "description": "First trading day the company was NOT a member.  NULL = current.  [) interval semantics — a company removed on 2017-06-19 is NOT a member on 2017-06-19."
        },
        "removal_announcement_date": {
          "type": "DATE",
          "description": "Date the removal was publicly announced; symmetric with announcement_date.  NULL when only the public-constituent-list / fund-snapshot layers wrote the row."
        },
        "removal_reason": {
          "type": "VARCHAR",
          "description": "merger | acquisition | bankruptcy | criteria | spinoff | other.  NULL when removal_date IS NULL."
        },
        "successor_cik": {
          "type": "VARCHAR",
          "description": "Acquirer CIK when removal_reason in {merger, acquisition}. Essential for backtests crossing M&A — without it, a stock disappearing looks indistinguishable from a delisting."
        },
        "source": {
          "type": "VARCHAR",
          "description": "Provenance — the column to branch on for how far a row can be trusted.  Live values with row counts (2026-09-16): ``fund_holdings`` 24,206 — publicly disclosed portfolio holdings of funds tracking each index, sole source of RUSSELL1000 / RUSSELL2000 / RUSSELL3000, always confidence='medium'; ``historical_seed`` 528 — curated S&P 500 spells 1996+; ``public_constituent_list`` 484 — current S&P 500 members, read from a public constituent list; ``succession_config`` 2 — curated reorg predecessor spells.  Valid but currently EMPTY: ``fund_snapshot``, ``manual``.  ⚠️ Every value names the MECHANISM that produced the row, never a publisher, fund or site — two were renamed on that principle (the Russell lane to ``fund_holdings`` on 2026-09-16, the current-members lane to ``public_constituent_list`` on 2026-09-17), so a snapshot cached before those dates carries a superseded string."
        },
        "confidence": {
          "type": "VARCHAR",
          "description": "high | medium | low.  Drives get_pit_universe's ``pit_safe`` flag, which is true only when EVERY matched row is 'high'.  In practice: all RUSSELL rows are 'medium' (fund-derived, observed at varying intervals), so any Russell query is pit_safe=false by construction; curated S&P 500 spells are 'high', dropping to 'low' for pre-2001 dates where the seed is least certain."
        },
        "notes": {
          "type": "VARCHAR"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        },
        "updated_at": {
          "type": "TIMESTAMPTZ"
        },
        "first_seen_date": {
          "type": "DATE",
          "description": "Panel metadata: the first date this constituent was actually OBSERVED in a holdings file.  Populated on every ``fund_holdings`` row (24,206), every ``public_constituent_list`` row (484) and 16 ``historical_seed`` rows; NULL on the rest of ``historical_seed`` and on ``succession_config``, which derive spells from a curated list rather than from an observed panel.  For a Russell row this is the honest observation window — ``effective_date`` may precede it only by the panel's observation granularity."
        },
        "last_seen_date": {
          "type": "DATE",
          "description": "Panel metadata — last date this constituent was observed in a holdings file.  Populated wherever ``first_seen_date`` is; NULL on the curated layers."
        },
        "avg_weight_pct": {
          "type": "DOUBLE",
          "description": "Panel metadata — average index weight (%) over the observed window.  Populated on 18,658 of the 24,206 ``fund_holdings`` rows — the dated holdings files carry weights, the earlier SEC-filed schedules do not; NULL elsewhere, including ``public_constituent_list``, which carries no weights."
        }
      }
    },
    "standard_concept": {
      "description": "Valuein's curated gold-standard concept catalog (~292 rows) — the dictionary for the ``fact.standard_concept`` column.  Each row is one L1/L2 concept (Revenue, GrossProfit, LongTermDebt, …) with its statement/category, definition, default unit, and Bloomberg/FactSet field equivalents.  Distinct from ``taxonomy_guide`` (the ~11,966-row raw SEC us-gaap tag reference for the ``fact.concept`` column): join ``fact.standard_concept = standard_concept.standard_concept`` for the curated label, ``fact.concept = taxonomy_guide.standard_concept`` for the raw tag.  The 'Other' catch-all row is included.",
      "columns": {
        "standard_concept": {
          "type": "VARCHAR",
          "primary_key": true
        },
        "level": {
          "type": "VARCHAR",
          "description": "L1 (top statement line) | L2 (sector-pack sub-line) | -- (the 'Other' catch-all)."
        },
        "statement_type": {
          "type": "VARCHAR",
          "description": "income | balance_sheet | cash_flow | comprehensive_income | supplemental | dei | notes | other."
        },
        "category": {
          "type": "VARCHAR",
          "description": "Finer grouping within the statement (e.g. revenue, working_capital, equity)."
        },
        "definition": {
          "type": "VARCHAR"
        },
        "unit_default": {
          "type": "VARCHAR",
          "description": "Canonical unit (USD | USD/share | shares | pct | …); NULL for non-numeric."
        },
        "is_flow": {
          "type": "BOOLEAN",
          "description": "TRUE for flow (period) measures, FALSE for stock (point-in-time) measures."
        },
        "parent_concept": {
          "type": "VARCHAR",
          "references": "standard_concept.standard_concept",
          "description": "Parent in the L1→L2 hierarchy (NULL for L1 / 'Other')."
        },
        "sector_pack": {
          "type": "VARCHAR",
          "description": "Sector pack a L2 concept belongs to (banking, insurance, reit, …); NULL unless level='L2'."
        },
        "bloomberg_equivalent": {
          "type": "VARCHAR",
          "description": "Closest Bloomberg field mnemonic, when one exists."
        },
        "factset_equivalent": {
          "type": "VARCHAR",
          "description": "Closest FactSet field, when one exists."
        },
        "gaap_ifrs_comparable": {
          "type": "BOOLEAN",
          "description": "TRUE when the concept maps cleanly across US-GAAP and IFRS filers."
        },
        "reviewed": {
          "type": "BOOLEAN",
          "description": "Human-in-the-loop CPA sign-off flag.  TRUE = an accountant has verified this concept's canonical name, statement_type and rule; the concept is then LOCKED — the standardization coverage loop only ever creates NEW concepts, never mutates a reviewed one.  Curated catalog rows default TRUE; loop-generated provisional rows are FALSE."
        },
        "reviewed_by": {
          "type": "VARCHAR",
          "description": "Who signed off (e.g. 'J. Smith, CPA'); NULL when not individually attributed."
        },
        "reviewed_at": {
          "type": "DATE",
          "description": "ISO date of the CPA sign-off; NULL when not individually attributed."
        },
        "review_confidence": {
          "type": "DOUBLE",
          "description": "Concept-level trust score in [0, 1] — the catalog analogue of ``fact.confidence_score``.  1.0 = CPA-verified (reviewed=TRUE); 0.7 = provisional loop output pending review.  Filter ``review_confidence >= 1.0`` for the accountant-verified concept set.  Distinct from the per-fact *match* accuracy."
        },
        "created_at": {
          "type": "TIMESTAMPTZ"
        },
        "updated_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "taxonomy_guide": {
      "description": "Human-readable definitions for all standard_concept values used in the fact table.",
      "columns": {
        "standard_concept": {
          "type": "VARCHAR",
          "primary_key": true
        },
        "human_name": {
          "type": "VARCHAR"
        },
        "definition": {
          "type": "VARCHAR"
        },
        "unit_type": {
          "type": "VARCHAR"
        },
        "balance_type": {
          "type": "VARCHAR"
        },
        "source_reference": {
          "type": "VARCHAR"
        },
        "level": {
          "type": "SMALLINT",
          "description": "Concept tier: 1 = L1 universal statement line, 2 = L2 sector-pack sub-line."
        },
        "statement_type": {
          "type": "VARCHAR",
          "description": "Statement the concept belongs to (income | balance_sheet | cash_flow | supplemental | …)."
        },
        "display_order": {
          "type": "SMALLINT",
          "description": "Suggested ordering within a statement for human-facing renders."
        },
        "is_active": {
          "type": "BOOLEAN",
          "description": "FALSE for retired concepts kept for historical fact lookups."
        },
        "gaap_ifrs_comparable": {
          "type": "BOOLEAN",
          "description": "TRUE when the concept maps cleanly across US-GAAP and IFRS filers."
        },
        "created_at": {
          "type": "TIMESTAMPTZ"
        },
        "updated_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "ratio": {
      "description": "Pipeline-computed financial ratios per entity per fiscal period. Derived from fact data.  Written append-on-restatement: each row's ``accepted_at`` is the PIT vintage = max(accepted_at) of the input facts that produced it, so a restated period (recomputed from a later-filed fact) is a NEW vintage row rather than an overwrite — mirroring fact.parquet.  Filter as-of with ``accepted_at <= as_of`` then take the latest vintage to avoid look-ahead bias.  No accession_id (ratios derive from multiple filings).  ``accepted_at`` is NULL for cross-sectional rank rows (``*_sector_pctile``) and any year whose source facts lacked an acceptance timestamp; ``computed_at`` tracks the latest computation within a vintage.",
      "columns": {
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "ratio_name": {
          "type": "VARCHAR"
        },
        "category": {
          "type": "VARCHAR",
          "description": "Groups ratios for filtering without scanning all ratio_name values. Values: profitability | per_share | liquidity | leverage | efficiency | forensic | growth | owner_earnings | valuation. ``valuation`` holds only OBSERVED or PARAMETER-FREE measures — the export-time price multiples (pe_ratio, pb_ratio, ev_ebitda, dividend_yield, market_cap) plus graham_number and ncav_per_share. It carries no intrinsic-value estimate: an intrinsic value depends on the caller's discount rate, terminal growth and horizon, so it is computed on demand (MCP ``compute_dcf``) and never published as a fact."
        },
        "value": {
          "type": "DOUBLE"
        },
        "confidence_score": {
          "type": "DOUBLE",
          "description": "Entity-period data quality: average fact.confidence_score of the facts feeding this period. Filter Bloomberg-grade with >= 0.95. NULL for cross-sectional rank rows (e.g. *_sector_pctile)."
        },
        "unit": {
          "type": "VARCHAR",
          "description": "percent | per_share | pure | x | percentile"
        },
        "period_end": {
          "type": "DATE"
        },
        "fiscal_year": {
          "type": "INTEGER"
        },
        "fiscal_period": {
          "type": "VARCHAR",
          "description": "FY | Q1 | Q2 | Q3 | Q4 | TTM"
        },
        "is_ttm": {
          "type": "BOOLEAN"
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ",
          "description": "PIT vintage — SEC acceptance timestamp of the latest input fact (max(accepted_at) of the year's facts).  The date this ratio first became knowable.  NULL for cross-sectional rank rows and un-timestamped years.  Filter ``accepted_at <= as_of`` for look-ahead-free backtests, mirroring fact.accepted_at."
        },
        "computed_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ",
          "description": "DB insertion timestamp; use ``computed_at`` for ratio freshness."
        }
      }
    },
    "restatement_events": {
      "description": "Restatement Radar feed — one row per financial fact that a later filing materially changed (>0.5% swing from the originally-reported value), across the full 1993→present archive.  Derived in DuckDB from fact.parquet by reproducing the fact_lineage_summary matview's aggregation over every vintage (see data-pipeline/services/restatements.py). Each row carries the original ('as reported') value, the current ('restated') value, the signed delta, a deterministic severity bucket, both filings' accessions for one-click SEC lineage, and a per-company magnitude rank ('Nth largest revision in this company's history').  Public-fact table — served to ALL tiers unsliced (copy-whole), including the free guest/sample tier, so the public /radar surface always shows the complete feed. ``event_id`` is a content hash of the tuple grain (cik, standard_concept, period_end, fiscal_period, unit), stable across re-restatements → stable /radar/[id] URL.",
      "columns": {
        "event_id": {
          "type": "VARCHAR",
          "primary_key": true,
          "description": "SHA-256 hex of cik|concept|period_end|fiscal_period|unit. Content-addressed on the tuple GRAIN (not the restating accession) so a fact restated twice keeps one stable id. Hashed over the RAW XBRL tag: hashing the canonical name would collide two different facts (many tags map to one standard_concept) onto one id and one /radar/[id] URL."
        },
        "cik": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "ticker": {
          "type": "VARCHAR",
          "description": "Primary listing symbol (references.symbol, is_primary_ticker)."
        },
        "company_name": {
          "type": "VARCHAR"
        },
        "sector": {
          "type": "VARCHAR"
        },
        "concept": {
          "type": "VARCHAR",
          "description": "The RAW XBRL tag both filings used (e.g. 'Revenues'). This is the detection grain AND the evidence the diff is apples-to-apples: a reader can open both accessions and confirm the same tag carries two different numbers. Diffing on standard_concept instead would compare values that were never comparable — see schema note 2.23.0."
        },
        "standard_concept": {
          "type": "VARCHAR",
          "description": "Canonical concept label (e.g. 'Revenue')."
        },
        "concept_label": {
          "type": "VARCHAR",
          "description": "As-reported label from the latest filing; falls back to standard_concept."
        },
        "statement_type": {
          "type": "VARCHAR",
          "description": "income_statement | balance_sheet | cash_flow (from the latest vintage)."
        },
        "period_start": {
          "type": "DATE",
          "description": "Start of the reporting WINDOW the restated fact covers — part of the detection grain since 2.25.0, and the reason one fact is no longer published as two.  Read straight from the XBRL context, so unlike `fiscal_period` it cannot disagree with itself across filings.  It also separates the one case fiscal_period existed to handle: a 3-month quarter and a 6-month YTD figure share a `period_end` but never a `period_start`.  NULL for instant (balance-sheet) facts, which have no duration."
        },
        "period_end": {
          "type": "DATE",
          "description": "End of the reporting period the restated fact covers."
        },
        "fiscal_period": {
          "type": "VARCHAR",
          "description": "FY | Q1 | Q2 | Q3 | Q4 — a DISPLAY LABEL, carried from the latest filing.  It is deliberately NOT part of the grain: it is missing on some vintages and wrong on others (real data: one period_end arrived tagged Q1, Q2 AND Q3), and an unreliable value in a grain splits one fact into several.  Use period_start/period_end for identity; use this to render it."
        },
        "unit": {
          "type": "VARCHAR"
        },
        "first_value": {
          "type": "DOUBLE",
          "description": "Value as ORIGINALLY reported (earliest accepted_at)."
        },
        "current_value": {
          "type": "DOUBLE",
          "description": "Value as RESTATED (latest accepted_at)."
        },
        "delta_abs": {
          "type": "DOUBLE",
          "description": "current_value − first_value."
        },
        "delta_pct": {
          "type": "DOUBLE",
          "description": "Signed relative change: (current − first) / |first|. Positive = revised up."
        },
        "version_count": {
          "type": "SMALLINT",
          "description": "Number of filings that reported this tuple (≥2 for a restatement)."
        },
        "first_filed_at": {
          "type": "TIMESTAMPTZ",
          "description": "accepted_at of the original filing."
        },
        "last_filed_at": {
          "type": "TIMESTAMPTZ",
          "description": "accepted_at of the restating filing — the event's detection time / feed ordering key."
        },
        "first_accession": {
          "type": "VARCHAR",
          "references": "filing.accession_id",
          "description": "Original filing (as-reported)."
        },
        "current_accession": {
          "type": "VARCHAR",
          "references": "filing.accession_id",
          "description": "Restating filing (restated)."
        },
        "restated_in_form": {
          "type": "VARCHAR",
          "description": "SEC form the RESTATING filing arrived in (10-K, 10-K/A, 10-Q, 10-Q/A, 20-F…). The single most important qualifier on an event: a 10-K/A is the company formally telling the SEC it got a number wrong; a routine next-year 10-K carrying a revised comparative is a re-file. Nullable — LEFT JOINed from filing, so an early vintage with no filing row still publishes."
        },
        "restated_in_amendment": {
          "type": "BOOLEAN",
          "description": "TRUE when the restating filing is an amendment (filing.is_amendment). The strongest signal a restatement is real and acknowledged, and the primary editorial filter for the public feed. FALSE when unknown — never claim an amendment we cannot prove."
        },
        "first_primary_doc": {
          "type": "VARCHAR",
          "description": "Primary document of the AS-REPORTED filing (e.g. 'goog-20251231.htm').  Lets a consumer build SEC's inline-XBRL viewer URL — /ix?doc=/Archives/edgar/data/{cik}/{accn}/{doc} — which opens the actual statement with every tagged fact live and searchable, instead of dumping the reader on a filing-index directory listing.  NULL when the filing row is unknown; the link then degrades to the index page."
        },
        "current_primary_doc": {
          "type": "VARCHAR",
          "description": "Primary document of the RESTATING filing.  See first_primary_doc — a diff has two sides and both should open on the page the number is actually printed on."
        },
        "disclosure_class": {
          "type": "VARCHAR",
          "description": "HOW the company told the market — 'non_reliance' | 'amended' | 'undisclosed'.  'non_reliance': an 8-K carrying Item 4.02 ('Non-Reliance on Previously Issued Financial Statements') was filed between the original and the restating filing — the company formally telling the SEC not to rely on what it had already reported.  The loudest signal in US disclosure, and rare (6 companies in the whole S&P 500 slice).  'amended': corrected in a 10-K/A or 10-Q/A.  'undisclosed': the value changed inside a ROUTINE periodic filing — no 4.02, no amendment; the old number is simply gone.  94.5% of events are 'undisclosed', which is the whole point of the column.  NOT NULL (defaults to 'undisclosed'): a missing disclosure is exactly what 'undisclosed' means, so there is no third state.  ⚠ It is NOT named 'silent' on purpose — see the 2.24.0 note at the top of this file.  It is a statement about the filing chain, not about the filer's intent."
        },
        "severity": {
          "type": "VARCHAR",
          "description": "Deterministic magnitude bucket: high (|Δ|≥10%) | medium (≥2%) | low (≥0.5%)."
        },
        "concept_importance": {
          "type": "SMALLINT",
          "description": "How much an analyst cares that THIS line moved: 1 headline (revenue, net income, EPS, total assets, operating cash flow), 2 primary statement line, 3 footnote (lease schedules, share counts, tax-rate reconciliations). Drives company_rank, so a revenue restatement outranks a footnote that happened to swing further."
        },
        "company_rank": {
          "type": "SMALLINT",
          "description": "Rank of this event within the company's restatement history (1 = most significant). Ordered by concept_importance FIRST, then |delta_pct|, then period_end — so a revenue restatement outranks a lease-schedule footnote that swung further. The feed keeps each company's top 10 (see FEED_TOP_N_PER_COMPANY: the full detection is ~1M rows, which OOMs the 128MB MCP Worker when read whole)."
        }
      }
    },
    "stock_price": {
      "description": "Coarse end-of-day market-price series per entity — period-end-aligned closes (one per fiscal period_end, for value-vs-price overlays) AND a monthly last-trading-day close history (for charting/backtest scaffolding).  Filter on ``observation`` to pick the grain.  RAW closes (never split/dividend-adjusted — split-invariant ratios need no adjustment); ``div_cash`` / ``split_factor`` carry the corporate-action factors for query-time total-return adjustment.  Point-in-time: ``accepted_at`` = the market-close timestamp, so an ``as_of`` before the close never sees that bar. GRAIN: one row per (entity_id, observation, period_end-or-month) — this table is joined to per-CIK fundamentals, so an issuer with several listed securities is resolved to its PRIMARY listing before alignment; ``security_id`` / ``symbol`` record which one, so a P/E is auditable back to the share class it was computed from.  For every listing of a multi-class issuer, or for total-return math at daily grain, read ``stock_price_daily`` instead (Institutional and Pro); at monthly grain use ``total_return_index`` on this table.  Derived at export from the internal licensed price archive.  ⚠️ COVERAGE FLOOR: licensed market data, NOT EDGAR — it does NOT start at EDGAR's 1993 floor.  No bars exist in 1993; the earliest bar differs per security and is 1994 or later (measured 2026-09-12).  Since schema 3.3.0 every row also carries ``total_return_index``, so the month-end rows are a total-return series on every tier — the free tiers' price series (the daily table ships to Institutional and Pro only).",
      "columns": {
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "security_id": {
          "type": "INTEGER",
          "references": "security.id",
          "description": "The listing this close came from — the issuer's primary ticker where SEC flags one, else its deepest-traded listing.  Added 2.29.0; before it, which share class priced a multi-class issuer was arbitrary."
        },
        "symbol": {
          "type": "VARCHAR",
          "description": "Ticker of ``security_id``, denormalized so no join is needed to read it."
        },
        "price_date": {
          "type": "DATE",
          "description": "Trading day the close is from."
        },
        "close": {
          "type": "DOUBLE",
          "description": "Raw (unadjusted) closing price."
        },
        "div_cash": {
          "type": "DOUBLE",
          "description": "Cash dividend with ex-date == price_date (else 0)."
        },
        "split_factor": {
          "type": "DOUBLE",
          "description": "Split factor on price_date (1.0 if none)."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ",
          "description": "Market-close PIT stamp for price_date; filter ``accepted_at <= as_of``."
        },
        "period_end": {
          "type": "DATE",
          "description": "Fiscal period this close aligns to (observation='period_end'); NULL for monthly rows."
        },
        "observation": {
          "type": "VARCHAR",
          "description": "'period_end' (close at/just-before a fiscal close) | 'monthly' (last trading day of a month)."
        },
        "total_return_index": {
          "type": "DOUBLE",
          "description": "The priced listing's total-return index at this close — the same series as ``stock_price_daily.total_return_index`` (forward-compounded from the listing's first archived bar, arbitrary base ≈ 1.0, dividends reinvested, splits neutralized, PIT-immutable), sampled at the coarse grain. Only RATIOS are meaningful: ``tri_b / tri_a - 1`` between two ``monthly`` rows is the holder's total return for that span, INCLUDING every dividend whose ex-date fell between them — which ``div_cash`` on a month-end row cannot tell you. NULL only where the daily series is NULL. Present on EVERY tier. Added 3.3.0."
        }
      }
    },
    "stock_price_daily": {
      "description": "Daily OHLCV bar series — one row per (SECURITY, trading day), NOT downsampled (unlike the coarse ``stock_price`` table).  Powers as-of-arbitrary-date price lookups (latest close on-or-before any calendar date) and total-return backtests.  ⚠️ GRAIN IS PER SECURITY, NOT PER COMPANY: an issuer with several listed securities (GOOG + GOOGL, a tracking stock, a contingent value right) contributes one row per listing, and both are real prices.  Partition or filter on ``security_id`` — a window function partitioned on ``entity_id`` alone alternates between two unrelated price series.  For one row per company per day, add ``WHERE is_primary_listing``.  TOTAL RETURN: use ``total_return_index``, not ``adjusted_close`` (which the vendor populates on only ~2% of bars) and never raw ``close`` (which drops ~75% on a 4-for-1 split).  Point-in-time: ``accepted_at`` = the market-close timestamp, so an ``as_of`` filter before the close never sees that bar.  Derived at export time from the internal licensed price archive via the same date-aware ticker→CIK resolution as ``stock_price``.  ⚠️ INSTITUTIONAL AND PRO ONLY (since schema 3.3.0): published to the ``full`` (all history) and ``pro`` (15-year window) buckets, sliced and per-CIK partitioned like the coarse table; NOT published to the Benchmark or Sample buckets, which carry only the coarse ``stock_price`` table and the price-derived valuation multiples in ``ratio``.  ⚠️ COVERAGE FLOOR: this is licensed market data, NOT EDGAR, so it does NOT start at EDGAR's 1993 floor.  No bars exist in 1993; the earliest bar differs per security and is 1994 or later (measured against production 2026-09-12).  A range before a security's first bar returns no rows on EVERY tier, Institutional included — that is data coverage, not a plan limit.",
      "columns": {
        "entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik"
        },
        "security_id": {
          "type": "INTEGER",
          "references": "security.id",
          "description": "The listing this bar belongs to.  ``(security_id, price_date)`` is the table's unique key — ``(entity_id, price_date)`` is NOT.  Added 2.29.0."
        },
        "symbol": {
          "type": "VARCHAR",
          "description": "Ticker of ``security_id``, denormalized so no join is needed to read it."
        },
        "is_primary_listing": {
          "type": "BOOLEAN",
          "description": "TRUE on exactly one bar per (entity_id, price_date): the issuer's representative listing.  Decided by SEC's primary-ticker flag, falling back to the deepest-traded listing, with the ticker breaking any remaining tie.  Filter on it to get one row per company per day without a join to ``security``."
        },
        "price_date": {
          "type": "DATE",
          "description": "Trading day the bar is from."
        },
        "open": {
          "type": "DOUBLE",
          "description": "Raw (unadjusted) opening price."
        },
        "high": {
          "type": "DOUBLE",
          "description": "Raw (unadjusted) intraday high."
        },
        "low": {
          "type": "DOUBLE",
          "description": "Raw (unadjusted) intraday low."
        },
        "close": {
          "type": "DOUBLE",
          "description": "Raw (unadjusted) closing price — PIT-immutable."
        },
        "adjusted_close": {
          "type": "DOUBLE",
          "description": "⚠️ SPARSE — the vendor populates this on roughly 2% of bars, so a query that depends on it returns mostly NULL.  Prefer ``total_return_index``, which is derived from columns that are complete.  Kept because where it IS present it is the vendor's own series and agrees with ours to r = 0.9999937.  Also note it is back-adjusted, so it is retroactively restated by every later corporate action and is NOT PIT-immutable."
        },
        "total_return_index": {
          "type": "DOUBLE",
          "description": "Total-return index for this listing, forward-compounded from its first bar in the archive (arbitrary base ≈ 1.0 — only RATIOS of it are meaningful).  Reinvests dividends and neutralizes splits: ``tri_b / tri_a - 1`` is the return a holder actually earned between two dates.  Built from ``close``/``div_cash``/``split_factor``, all of which are 100% populated, using the vendor's multiplicative convention ``close_t * split_t / (close_t-1 - div_t)``.  PIT-IMMUTABLE, unlike a back-adjusted price series: tomorrow's dividend appends a new value rather than rewriting today's, so a backtest run last year still reproduces.  Compounds per SECURITY — a ticker RENAME starts a new series, because the price archive is keyed on ticker and genuinely has no bar linking the two."
        },
        "volume": {
          "type": "BIGINT",
          "description": "Shares traded on price_date."
        },
        "div_cash": {
          "type": "DOUBLE",
          "description": "Cash dividend with ex-date == price_date (else 0)."
        },
        "split_factor": {
          "type": "DOUBLE",
          "description": "Split factor on price_date (1.0 if none)."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ",
          "description": "Market-close PIT stamp for price_date; filter ``accepted_at <= as_of``."
        }
      }
    },
    "insider_party": {
      "description": "Directory of every insider / reporting person referenced by Form 3/4/5/144.  Keyed on CIK when SEC-registered, otherwise on a normalized name.  Joined to insider_transaction via insider_party_id.  FULL bucket only — enterprise tier (full plan).",
      "columns": {
        "id": {
          "type": "INTEGER",
          "primary_key": true
        },
        "cik": {
          "type": "VARCHAR",
          "description": "10-digit zero-padded SEC CIK; NULL for filers without an SEC-registered identity (rare individual reporters)."
        },
        "name": {
          "type": "VARCHAR"
        },
        "normalized_name": {
          "type": "VARCHAR",
          "description": "Uppercased + whitespace-collapsed name for CIK-less filer dedup."
        },
        "party_type": {
          "type": "VARCHAR",
          "description": "person | entity"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "insider_filing": {
      "description": "Form 3 / 4 / 5 / SC 13D / SC 13G / 144 (and amendments) — the subject-rooted insider DISCLOSURE filing record.  Distinct from ``filing`` (financial-statement forms) so the financial table stays clean.  ``subject_cik`` is the issuer the disclosure is about — a SOFT reference (LEFT JOIN to entity.cik when in the fundamentals universe; may be NULL/unmatched for foreign / pre-IPO / delisted issuers).  ``filer_party_id`` FKs to ``insider_party`` (the insider / holder).  Children ``insider_transaction`` and ``insider_ownership`` FK to this table via accession_id.  FULL bucket only — enterprise tier.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "accession_id": {
          "type": "VARCHAR",
          "description": "Unique SEC accession ID for this disclosure filing."
        },
        "subject_cik": {
          "type": "VARCHAR",
          "description": "Issuer CIK the disclosure is about — soft reference (LEFT JOIN to entity.cik; no hard FK)."
        },
        "filer_party_id": {
          "type": "BIGINT",
          "references": "insider_party.id",
          "nullable": true,
          "description": "FK to the insider / holder in insider_party (NULL when unresolved)."
        },
        "filer_cik": {
          "type": "VARCHAR",
          "description": "Denormalised filer CIK for query convenience."
        },
        "form_type": {
          "type": "VARCHAR"
        },
        "filing_date": {
          "type": "DATE"
        },
        "report_date": {
          "type": "DATE"
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "is_amendment": {
          "type": "BOOLEAN"
        },
        "amendment_no": {
          "type": "SMALLINT"
        },
        "superseded_by": {
          "type": "VARCHAR"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "insider_transaction": {
      "description": "Form 3 / 4 / 5 / 144 line items — each row is one transaction, initial holding, or proposed sale by an insider.  transaction_type is 'transaction' (Form 4/5), 'initial_holding' (Form 3), or 'proposed_sale' (Form 144).  Joins to ``insider_filing`` via accession_id, to ``entity`` (issuer) via entity_id (soft ref), and to ``insider_party`` via insider_party_id.  FULL bucket only.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "accession_id": {
          "type": "VARCHAR",
          "references": "insider_filing.accession_id"
        },
        "transaction_line_no": {
          "type": "SMALLINT"
        },
        "entity_id": {
          "type": "VARCHAR",
          "description": "Issuer CIK (the company whose securities the insider holds).  Soft reference — LEFT JOIN to entity.cik when the issuer is in the fundamentals universe; may not resolve for foreign / pre-IPO / delisted issuers."
        },
        "insider_party_id": {
          "type": "INTEGER",
          "references": "insider_party.id"
        },
        "form_type": {
          "type": "VARCHAR"
        },
        "transaction_type": {
          "type": "VARCHAR",
          "description": "transaction | initial_holding | proposed_sale"
        },
        "transaction_date": {
          "type": "DATE"
        },
        "transaction_code": {
          "type": "VARCHAR"
        },
        "security_title": {
          "type": "VARCHAR"
        },
        "is_derivative": {
          "type": "BOOLEAN"
        },
        "shares": {
          "type": "DOUBLE"
        },
        "price_per_share": {
          "type": "DOUBLE"
        },
        "post_transaction_shares": {
          "type": "DOUBLE"
        },
        "ownership_form": {
          "type": "VARCHAR",
          "description": "D = direct, I = indirect."
        },
        "indirect_ownership_nature": {
          "type": "VARCHAR"
        },
        "insider_role": {
          "type": "ARRAY<VARCHAR>",
          "description": "Officer | Director | TenPercentOwner | Other (multi-valued)."
        },
        "officer_title": {
          "type": "VARCHAR"
        },
        "aggregate_market_value": {
          "type": "DOUBLE",
          "description": "Form 144 only — proposed-sale total dollar value."
        },
        "broker_name": {
          "type": "VARCHAR",
          "description": "Form 144 only."
        },
        "approximate_sale_date": {
          "type": "DATE",
          "description": "Form 144 only."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        },
        "insider_name": {
          "type": "VARCHAR",
          "description": "DENORMALIZED from insider_party.name (LEFT JOIN on insider_party_id at export). Inlined so the MCP Worker resolves the reporting insider's name from this tiny per-CIK partition without decoding the 406K-row insider_party dimension (the CF 1102 OOM). NULL if the party_id has no dimension match."
        },
        "insider_cik": {
          "type": "VARCHAR",
          "description": "DENORMALIZED from insider_party.cik (see insider_name)."
        }
      }
    },
    "institutional_filing": {
      "description": "Form 13F-HR / 13F-NT (and amendments) — the manager-rooted filing record.  Distinct from ``filing`` (issuer-rooted) because 13F filings have no single subject issuer (the manager reports a portfolio across many issuers).  FK to ``insider_party`` for the manager.  FULL bucket only — enterprise tier.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "accession_id": {
          "type": "VARCHAR",
          "description": "Unique SEC accession ID for this 13F filing."
        },
        "filer_party_id": {
          "type": "BIGINT",
          "references": "insider_party.id",
          "description": "FK to the manager's row in insider_party."
        },
        "filer_cik": {
          "type": "VARCHAR",
          "description": "Denormalised manager CIK — mirrors institutional_holding.filer_cik for query convenience."
        },
        "filing_date": {
          "type": "DATE"
        },
        "form_type": {
          "type": "VARCHAR"
        },
        "period_end": {
          "type": "DATE",
          "description": "PERIODOFREPORT — quarter-end the holdings reflect."
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "is_amendment": {
          "type": "BOOLEAN"
        },
        "amendment_no": {
          "type": "SMALLINT"
        },
        "superseded_by": {
          "type": "VARCHAR"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "institutional_holding": {
      "description": "Form 13F-HR holdings — institutional managers' quarterly position disclosures.  One row per (filing, CUSIP, share-class, put/call).  Joins to ``institutional_filing`` via accession_id (the 13F filing record); ``filer_cik`` is the manager CIK (denormalised — same value as institutional_filing.filer_cik). ``subject_entity_id`` is the issuer CIK (NULL until the CUSIP→CIK lookup resolves).  FULL bucket only — enterprise tier.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "accession_id": {
          "type": "VARCHAR",
          "references": "institutional_filing.accession_id"
        },
        "filer_cik": {
          "type": "VARCHAR",
          "description": "Manager CIK (denormalised from institutional_filing.filer_cik for query convenience)."
        },
        "filer_name": {
          "type": "VARCHAR",
          "description": "DENORMALIZED 13F manager name (LEFT JOIN insider_party on filer_cik = cik at export time). Lets consumers render the holder name without decoding the insider_party dimension. NULL only when the manager CIK is unresolved."
        },
        "period_end": {
          "type": "DATE",
          "description": "Reporting quarter-end."
        },
        "cusip": {
          "type": "VARCHAR"
        },
        "subject_entity_id": {
          "type": "VARCHAR",
          "references": "entity.cik",
          "description": "Resolved issuer CIK; NULL until CUSIP→CIK lookup completes."
        },
        "name_of_issuer": {
          "type": "VARCHAR"
        },
        "title_of_class": {
          "type": "VARCHAR"
        },
        "shares": {
          "type": "DOUBLE"
        },
        "market_value_usd": {
          "type": "DOUBLE"
        },
        "put_call": {
          "type": "VARCHAR",
          "description": "PUT | CALL | NULL (NULL = common stock, not an option)."
        },
        "investment_discretion": {
          "type": "VARCHAR",
          "description": "SOLE | DFND | OTR (Form 13F bulk vocabulary) or SOLE | SHARED | NONE (per-filing XML vocabulary)."
        },
        "sole_voting": {
          "type": "DOUBLE"
        },
        "shared_voting": {
          "type": "DOUBLE"
        },
        "no_voting": {
          "type": "DOUBLE"
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "insider_ownership": {
      "description": "SC 13D / SC 13G beneficial-ownership disclosures (5%+ stakes).  One row per (filing, reporting person).  Group filings carry multiple reporting persons — each lands as a separate row.  Joins to ``insider_filing`` via accession_id; subject_entity_id is the issuer CIK (the company being held — soft ref, LEFT JOIN to entity).  FULL bucket only — enterprise tier (full plan).",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "accession_id": {
          "type": "VARCHAR",
          "references": "insider_filing.accession_id"
        },
        "filer_cik": {
          "type": "VARCHAR",
          "description": "Reporting-person CIK; NULL when not SEC-registered (many institutional disclosures only carry a name)."
        },
        "filer_party_id": {
          "type": "BIGINT",
          "references": "insider_party.id",
          "nullable": true,
          "description": "FK into ``insider_party`` for the reporting person.  Resolved at merge time from the cover-page filer name via normalized_name match.  Use this column for cross-filing roll-ups (\"every disclosure by this filer over time\"); ``filer_name`` stays as the verbatim cover-page text for display."
        },
        "filer_name": {
          "type": "VARCHAR"
        },
        "filer_type": {
          "type": "VARCHAR",
          "description": "institution | individual | group | NULL."
        },
        "subject_entity_id": {
          "type": "VARCHAR",
          "description": "Issuer CIK — the company whose stock is held.  Soft reference — LEFT JOIN to entity.cik when in the fundamentals universe."
        },
        "schedule_type": {
          "type": "VARCHAR",
          "description": "SC 13D | SC 13D/A | SC 13G | SC 13G/A (or SCHEDULE 13… long-form variants — both vocabularies accepted)."
        },
        "as_of_date": {
          "type": "DATE"
        },
        "percent_owned": {
          "type": "DOUBLE"
        },
        "shares_owned": {
          "type": "DOUBLE"
        },
        "sole_voting_power": {
          "type": "DOUBLE"
        },
        "shared_voting_power": {
          "type": "DOUBLE"
        },
        "sole_dispositive_power": {
          "type": "DOUBLE"
        },
        "shared_dispositive_power": {
          "type": "DOUBLE"
        },
        "accepted_at": {
          "type": "TIMESTAMPTZ"
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "investment_adviser": {
      "description": "SEC Form ADV Part 1A — registered investment advisers and Exempt Reporting Advisers (~23.6K firms).  A SEPARATE SEC SOURCE from EDGAR: advisers file into IARD (operated by FINRA Regulation), so rows key on the firm's CRD number, NOT a CIK.  Sourced from the daily IAPD bulk feed ``IA_FIRM_SEC_Feed_*.xml.gz``.\n\nPIT: the natural key is (crd_number, filing_date) where filing_date is the adviser's own Form ADV filing date.  A firm's annual updating amendment APPENDS a new vintage instead of overwriting the prior one — the same append-on-restatement shape as ``ratio.accepted_at``.  Filter ``filing_date <= as_of`` and take the latest vintage per crd_number for a point-in-time view, or the whole partition for adviser history.\n\n⚠ Items 5.A–5.F are ABSENT for firm_type='ERA' — Exempt Reporting Advisers file an abbreviated form.  That is a different filing obligation, NOT missing data; scope coverage metrics to firm_type='Registered' or you understate completeness by ~28%.\n\nFULL (Institutional) bucket only.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "crd_number": {
          "type": "VARCHAR",
          "description": "IARD firm identifier (Item 1.E.(1)) — the natural key. Not a CIK."
        },
        "filing_date": {
          "type": "DATE",
          "description": "PIT vintage — the date the adviser filed this Form ADV."
        },
        "sec_number": {
          "type": "VARCHAR",
          "description": "SEC file number, 801-/802- (Item 1.O)."
        },
        "business_name": {
          "type": "VARCHAR",
          "description": "Doing-business-as name (Item 1.B)."
        },
        "legal_name": {
          "type": "VARCHAR",
          "description": "Legal name (Item 1.A)."
        },
        "sec_region": {
          "type": "VARCHAR",
          "description": "SEC regional office code, e.g. NYRO."
        },
        "umbrella_registration": {
          "type": "BOOLEAN",
          "description": "Filing covers a group of relying advisers under one umbrella registration."
        },
        "cik": {
          "type": "VARCHAR",
          "references": "entity.cik",
          "description": "SOFT link to the EDGAR issuer universe (Item 1.D.(3)).  NULL for most advisers — they are not EDGAR filers — so a NULL is expected, not an error.  Populated only from the monthly FOIA bundles (~26% of filers); the daily XML feed never carries it.  This is the ONLY bridge from an adviser to entity / institutional_holding."
        },
        "main_address": {
          "type": "VARCHAR",
          "description": "Principal office street address (Item 1.F)."
        },
        "main_city": {
          "type": "VARCHAR"
        },
        "main_state": {
          "type": "VARCHAR"
        },
        "main_country": {
          "type": "VARCHAR"
        },
        "main_postal_code": {
          "type": "VARCHAR"
        },
        "mailing_address": {
          "type": "VARCHAR",
          "description": "Mailing address (Item 1.G)."
        },
        "phone": {
          "type": "VARCHAR"
        },
        "fax": {
          "type": "VARCHAR"
        },
        "websites": {
          "type": "JSON",
          "description": "Array of web addresses (Item 1.I)."
        },
        "firm_type": {
          "type": "VARCHAR",
          "description": "'Registered' (SEC-registered adviser) | 'ERA' (Exempt Reporting Adviser). ERAs file an abbreviated Form ADV — Items 5.A–5.F are NULL for them."
        },
        "registration_status": {
          "type": "VARCHAR",
          "description": "APPROVED | ACTIVE | APPROVED-120 (120-day provisional)."
        },
        "registration_date": {
          "type": "DATE"
        },
        "form_version": {
          "type": "VARCHAR",
          "description": "Form ADV revision used, e.g. '10/2021'."
        },
        "feed_generated_on": {
          "type": "DATE",
          "description": "Date the IAPD bulk snapshot was generated (feed freshness)."
        },
        "org_form": {
          "type": "VARCHAR",
          "description": "Legal form of organization (Item 3.A)."
        },
        "fiscal_year_end": {
          "type": "VARCHAR",
          "description": "Fiscal year-end month (Item 3.B)."
        },
        "org_state": {
          "type": "VARCHAR"
        },
        "org_country": {
          "type": "VARCHAR"
        },
        "total_employees": {
          "type": "BIGINT",
          "description": "Item 5.A — total employees."
        },
        "employees_advisory_research": {
          "type": "BIGINT",
          "description": "Item 5.B.(1) — employees who perform investment advisory functions INCLUDING RESEARCH.  The in-house research-capacity measure; NULL for ERAs."
        },
        "employees_registered_reps": {
          "type": "BIGINT",
          "description": "Item 5.B.(2) — registered representatives of a broker-dealer."
        },
        "employees_state_iar": {
          "type": "BIGINT",
          "description": "Item 5.B.(3) — state-registered investment adviser representatives."
        },
        "employees_bd_agents": {
          "type": "BIGINT",
          "description": "Item 5.B.(4) — state-registered broker-dealer agents."
        },
        "employees_insurance_agents": {
          "type": "BIGINT",
          "description": "Item 5.B.(5) — licensed insurance agents."
        },
        "employees_solicitors": {
          "type": "BIGINT",
          "description": "Item 5.B.(6) — solicitors."
        },
        "clients_without_raum": {
          "type": "BIGINT",
          "description": "Item 5.C.(1) — clients for whom the adviser reports NO regulatory AUM."
        },
        "pct_clients_non_us": {
          "type": "BIGINT",
          "description": "Item 5.C.(2) — percent of clients who are non-US persons (0-100)."
        },
        "client_type_counts": {
          "type": "JSON",
          "description": "Item 5.D client counts keyed by category: individuals, hnw_individuals, banking_thrift, investment_companies, business_development_companies, pooled_investment_vehicles, pension_profit_sharing, charitable_organizations, government_entities, other_investment_advisers, insurance_companies, sovereign_wealth_funds, corporations, other.  Absent key = not reported."
        },
        "client_type_raum": {
          "type": "JSON",
          "description": "Item 5.D regulatory AUM by the same category keys as client_type_counts. VERIFIED IDENTITY: these values sum EXACTLY to raum_total (checked at ingest across every adviser reporting both — 100% agreement)."
        },
        "client_type_other_desc": {
          "type": "VARCHAR",
          "description": "Item 5.D.(n) free-text label."
        },
        "comp_aum_pct": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(1) — percentage of AUM."
        },
        "comp_hourly": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(2) — hourly charges."
        },
        "comp_subscription": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(3) — subscription fees."
        },
        "comp_fixed": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(4) — fixed fees."
        },
        "comp_commission": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(5) — commissions."
        },
        "comp_performance": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(6) — performance-based fees."
        },
        "comp_other": {
          "type": "BOOLEAN",
          "description": "Item 5.E.(7) — other."
        },
        "comp_other_desc": {
          "type": "VARCHAR"
        },
        "reports_raum": {
          "type": "BOOLEAN",
          "description": "Item 5.F.(1) — does the adviser report RAUM."
        },
        "raum_discretionary": {
          "type": "DOUBLE",
          "description": "Item 5.F.(2)(a) — discretionary RAUM (USD)."
        },
        "raum_non_discretionary": {
          "type": "DOUBLE",
          "description": "Item 5.F.(2)(b) — non-discretionary."
        },
        "raum_total": {
          "type": "DOUBLE",
          "description": "Item 5.F.(2)(c) — total regulatory assets under management (USD).\n⚠ STRUCTURALLY DOUBLE-COUNTED across sub-advisory and affiliated-adviser relationships: the same dollars are reported by every adviser in the chain, so summing this column across firms yields ~$177T, far above global AUM. Valid per-firm and for ranking/screening; NEVER aggregate it."
        },
        "accounts_discretionary": {
          "type": "BIGINT",
          "description": "Item 5.F.(2)(d)."
        },
        "accounts_non_discretionary": {
          "type": "BIGINT",
          "description": "Item 5.F.(2)(e)."
        },
        "accounts_total": {
          "type": "BIGINT",
          "description": "Item 5.F.(2)(f) — total advisory accounts."
        },
        "raum_non_us_clients": {
          "type": "DOUBLE",
          "description": "Item 5.F.(3) — RAUM of non-US clients."
        },
        "advisory_activities": {
          "type": "JSON",
          "description": "Item 5.G.(1)-(12) service-type booleans: financial_planning, portfolio_mgmt_individuals, portfolio_mgmt_investment_companies, portfolio_mgmt_pooled_vehicles, portfolio_mgmt_businesses, pension_consulting, selection_of_other_advisers, publication_of_periodicals, security_ratings_or_pricing, market_timing_services, educational_seminars, other."
        },
        "financial_planning_clients": {
          "type": "VARCHAR",
          "description": "Item 5.H — financial-planning client count, published by the SEC as a BUCKET string ('0', '1-10', '11-25', '26-50', '51-100', '101-250', '251-500', 'More than 500'), NOT an integer.  Stored verbatim; do not cast to a number."
        },
        "affiliations": {
          "type": "JSON",
          "description": "Item 7.A.(1)-(16) financial-industry affiliation booleans: broker_dealer, registered_rep_of_broker_dealer, commodity_pool_operator_or_cta, futures_commission_merchant, real_estate_broker_or_agent, insurance_company_or_agency, pension_consultant, trust_company, registered_municipal_advisor, registered_sb_swap_dealer, major_sb_swap_participant, accountant_or_accounting_firm, lawyer_or_law_firm, banking_or_thrift_institution, sponsor_or_syndicator_of_lps, other_investment_adviser_or_financial_planner."
        },
        "has_private_funds": {
          "type": "BOOLEAN",
          "description": "Item 7.B flag — does the adviser advise any private fund.  Fund-level detail lives in investment_adviser_private_fund, which is sourced from a DIFFERENT file; a TRUE here with no child rows means that adviser's Schedule D has not been ingested yet, not that the fund detail is missing from the SEC."
        },
        "source": {
          "type": "VARCHAR",
          "description": "Provenance: 'ia_firm_sec_feed'."
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    },
    "investment_adviser_private_fund": {
      "description": "Form ADV Schedule D, Section 7.B.(1) — private funds advised by a registered investment adviser.  One row per (adviser, fund, filing vintage); joins to ``investment_adviser`` on crd_number.\n\n⚠ SOURCE + COVERAGE: the daily IAPD feed carries only the Item 7.B yes/no flag, so fund detail comes from the MONTHLY FOIA bundles (``ADV_Filing_Data_YYYYMMDD_YYYYMMDD.zip``), which are a FILING-EVENT stream — roughly 2.9K of 23.6K advisers per month.  Because each adviser files one annual updating amendment, coverage converges on the full fund-reporting population only across ~12 monthly bundles.  A partial load looks complete but is not: check ``investment_adviser.has_private_funds`` for the authoritative population count.\n\nFULL (Institutional) bucket only.",
      "columns": {
        "id": {
          "type": "BIGINT",
          "primary_key": true
        },
        "crd_number": {
          "type": "VARCHAR",
          "references": "investment_adviser.crd_number",
          "description": "Advising firm's CRD number (soft ref — the parent is vintage-keyed)."
        },
        "fund_id": {
          "type": "VARCHAR",
          "description": "SEC private-fund identifier, e.g. '805-4964869201'. Stable across filings."
        },
        "filing_date": {
          "type": "DATE",
          "description": "PIT vintage — filing date of the reporting ADV."
        },
        "filing_id": {
          "type": "VARCHAR",
          "description": "IARD FilingID the row was parsed from."
        },
        "reference_id": {
          "type": "VARCHAR",
          "description": "Per-filing fund sequence id; join key to the Schedule D sub-files."
        },
        "fund_name": {
          "type": "VARCHAR"
        },
        "fund_type": {
          "type": "VARCHAR",
          "description": "Hedge Fund | Private Equity Fund | Real Estate Fund | Venture Capital Fund | Liquidity Fund | Securitized Asset Fund | Other Private Fund."
        },
        "fund_type_other": {
          "type": "VARCHAR"
        },
        "state": {
          "type": "VARCHAR",
          "description": "Organizing jurisdiction (state or foreign)."
        },
        "country": {
          "type": "VARCHAR"
        },
        "gross_asset_value": {
          "type": "DOUBLE",
          "description": "Fund gross asset value in USD (Schedule D 7.B.(1)(A)(11)).  Unlike the adviser RAUM column this is fund-level and NOT double-counted across advisers, except for master/feeder pairs — filter is_feeder_fund to avoid counting a feeder's assets alongside its master's."
        },
        "minimum_investment": {
          "type": "DOUBLE"
        },
        "owner_count": {
          "type": "BIGINT",
          "description": "Approximate number of beneficial owners."
        },
        "pct_owned_non_us": {
          "type": "BIGINT",
          "description": "Percent owned by non-US persons (0-100)."
        },
        "exclusion_3c1": {
          "type": "BOOLEAN",
          "description": "Relies on the Investment Company Act §3(c)(1) exclusion (≤100 holders)."
        },
        "exclusion_3c7": {
          "type": "BOOLEAN",
          "description": "Relies on the §3(c)(7) exclusion (qualified purchasers only)."
        },
        "is_master_fund": {
          "type": "BOOLEAN"
        },
        "is_feeder_fund": {
          "type": "BOOLEAN",
          "description": "TRUE = feeder in a master-feeder structure; exclude when summing GAV."
        },
        "master_fund_name": {
          "type": "VARCHAR"
        },
        "master_fund_id": {
          "type": "VARCHAR"
        },
        "is_fund_of_funds": {
          "type": "BOOLEAN"
        },
        "has_annual_audit": {
          "type": "BOOLEAN",
          "description": "Fund is subject to an annual audit."
        },
        "is_gaap": {
          "type": "BOOLEAN",
          "description": "Financial statements prepared under US GAAP."
        },
        "unqualified_opinion": {
          "type": "BOOLEAN",
          "description": "Most recent audit produced an unqualified opinion."
        },
        "uses_prime_broker": {
          "type": "BOOLEAN",
          "description": "Fund uses a prime broker.  A PRESENCE flag — the SEC reports Y/N here, not a name."
        },
        "uses_custodian": {
          "type": "BOOLEAN",
          "description": "Fund uses a custodian (presence flag)."
        },
        "uses_administrator": {
          "type": "BOOLEAN",
          "description": "Fund uses an administrator (presence flag)."
        },
        "auditor_name": {
          "type": "VARCHAR",
          "description": "Auditing firm name, resolved from Schedule D 7.B.(1)(A)(23)."
        },
        "auditor_pcaob_registered": {
          "type": "BOOLEAN",
          "description": "Auditor is registered with the PCAOB (7.B.(1)(A)(23))."
        },
        "source": {
          "type": "VARCHAR",
          "description": "Provenance: 'adv_filing_data'."
        },
        "ingested_at": {
          "type": "TIMESTAMPTZ"
        }
      }
    }
  }
}
